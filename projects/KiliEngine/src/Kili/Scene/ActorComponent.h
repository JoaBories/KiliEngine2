#pragma once

/**
 * Actor Component, object representing a behavior that will be linked to a specific actor.
 * Example : mesh component, rigidbody component, collider component, etc.
 */
class ActorComponent
{
public:
    
};

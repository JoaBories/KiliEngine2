#pragma once

/**
 * Game System, object representing a game system that don't have a transform.
 * Example : scene sound system, voxel world system, ui system, etc.
 */
class GameSystem
{
public:
    
};

#pragma once

/**
 * Game Actor, object that act as a collection of Actor Components and populate the scene.
 * Example : camera, mesh actor, player, enemy, etc.
 */
class GameActor
{
public:
    
};
